<div class="max-w-2xl mx-auto space-y-6">

  <div>
    <h2 class="text-2xl font-bold text-[#2D4BA6] dark:text-white">Perfil da empresa</h2>
    <p class="text-sm text-slate-500 dark:text-white/60 mt-0.5">Atualize as informações da sua empresa</p>
  </div>

  {{#if success}}
  <div class="px-4 py-3 rounded-xl bg-emerald-50 dark:bg-emerald-900/20 border border-emerald-200 dark:border-emerald-800 text-emerald-700 dark:text-emerald-300 text-sm font-medium">
    Perfil atualizado com sucesso!
  </div>
  {{/if}}

  <form method="POST" action="/empresa/perfil" enctype="multipart/form-data"
    class="bg-white dark:bg-[#102235] border border-slate-200 dark:border-white/10 rounded-2xl p-6 shadow-sm space-y-5">

    <!-- Logo atual -->
    <div class="flex items-center gap-5">
      {{#if empresa.logo}}
      <img src="{{empresa.logo}}" class="w-20 h-20 rounded-2xl object-cover border border-slate-200 dark:border-white/10 shadow-sm" />
      {{else}}
      <div class="w-20 h-20 rounded-2xl bg-violet-100 dark:bg-violet-900/30 flex items-center justify-center text-violet-600 dark:text-violet-300 font-bold text-2xl shadow-sm">
        {{empresa.nome_fantasia.[0]}}
      </div>
      {{/if}}
      <div class="flex-1">
        <label class="block text-sm font-semibold text-slate-700 dark:text-white/80 mb-1">Logo da empresa</label>
        <input type="file" name="file" accept="image/jpeg,image/png,image/webp"
          class="w-full text-sm text-slate-500 dark:text-white/50 file:mr-3 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-violet-100 dark:file:bg-violet-900/30 file:text-violet-700 dark:file:text-violet-300 hover:file:opacity-80" />
        <p class="text-xs text-slate-400 dark:text-white/30 mt-1">JPG, PNG ou WebP · máx. 3MB</p>
      </div>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      <div>
        <label class="block text-sm font-semibold text-slate-700 dark:text-white/80 mb-1">Nome fantasia *</label>
        <input type="text" name="nome_fantasia" value="{{empresa.nome_fantasia}}" required maxlength="255"
          class="w-full px-4 py-2.5 rounded-xl border border-slate-200 dark:border-white/10 bg-slate-50 dark:bg-white/5 text-slate-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-violet-400" />
      </div>
      <div>
        <label class="block text-sm font-semibold text-slate-700 dark:text-white/80 mb-1">Razão social</label>
        <input type="text" name="razao_social" value="{{empresa.razao_social}}" maxlength="255"
          class="w-full px-4 py-2.5 rounded-xl border border-slate-200 dark:border-white/10 bg-slate-50 dark:bg-white/5 text-slate-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-violet-400" />
      </div>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      <div>
        <label class="block text-sm font-semibold text-slate-700 dark:text-white/80 mb-1">Telefone</label>
        <input type="tel" name="telefone" value="{{empresa.telefone}}" maxlength="20"
          class="w-full px-4 py-2.5 rounded-xl border border-slate-200 dark:border-white/10 bg-slate-50 dark:bg-white/5 text-slate-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-violet-400" />
      </div>
      <div>
        <label class="block text-sm font-semibold text-slate-700 dark:text-white/80 mb-1">Setor de atuação</label>
        <input type="text" name="setor" value="{{empresa.setor}}" maxlength="100" placeholder="Ex: Tecnologia, Saúde..."
          class="w-full px-4 py-2.5 rounded-xl border border-slate-200 dark:border-white/10 bg-slate-50 dark:bg-white/5 text-slate-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-violet-400" />
      </div>
    </div>

    <div>
      <label class="block text-sm font-semibold text-slate-700 dark:text-white/80 mb-1">Descrição</label>
      <textarea name="descricao" rows="4" maxlength="1000" placeholder="Conte um pouco sobre sua empresa..."
        class="w-full px-4 py-2.5 rounded-xl border border-slate-200 dark:border-white/10 bg-slate-50 dark:bg-white/5 text-slate-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-violet-400 resize-none">{{empresa.descricao}}</textarea>
    </div>

    <!-- Email (somente leitura) -->
    <div>
      <label class="block text-sm font-semibold text-slate-700 dark:text-white/80 mb-1">E-mail</label>
      <input type="email" value="{{empresa.email}}" disabled
        class="w-full px-4 py-2.5 rounded-xl border border-slate-200 dark:border-white/10 bg-slate-100 dark:bg-white/5 text-slate-400 dark:text-white/40 text-sm cursor-not-allowed" />
      <p class="text-xs text-slate-400 dark:text-white/30 mt-1">O e-mail não pode ser alterado.</p>
    </div>

    <!-- CNPJ (somente leitura) -->
    <div>
      <label class="block text-sm font-semibold text-slate-700 dark:text-white/80 mb-1">CNPJ</label>
      <input type="text" value="{{empresa.cnpj}}" disabled
        class="w-full px-4 py-2.5 rounded-xl border border-slate-200 dark:border-white/10 bg-slate-100 dark:bg-white/5 text-slate-400 dark:text-white/40 text-sm cursor-not-allowed" />
    </div>

    <div class="flex gap-3 pt-2">
      <button type="submit"
        class="flex-1 py-3 rounded-2xl bg-gradient-to-r from-violet-500 to-purple-700 text-white font-bold text-sm shadow hover:opacity-90 transition">
        Salvar alterações
      </button>
    </div>
  </form>

</div>
